import React from "react";
import ScheduleCard from "./ScheduleCard";

const ScheduleList = () => {
  return (
    <div className="schedule-list">
      <ul>Schedule List Here!</ul>
      <ScheduleCard />
    </div>
  );
};

export default ScheduleList;
