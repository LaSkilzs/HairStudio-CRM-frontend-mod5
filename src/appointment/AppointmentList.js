import React from "react";
import AppointmentCard from "./AppointmentCard";

const AppointmentList = () => {
  return (
    <div className="appointment-list">
      <ul>Appointment List is here</ul>
      <AppointmentCard />
    </div>
  );
};

export default AppointmentList;
